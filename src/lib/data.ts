
// This file is no longer used for initial data as the application is now integrated with Firestore.
// It is kept to avoid breaking imports in other files, but the arrays are empty.

import type { Transaction, InventoryItem } from '@/lib/types';

export const inventory: InventoryItem[] = [];
export const transactions: Transaction[] = [];
